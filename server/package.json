{
  "name": "resumetailor-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ResumeTailor AI backend: parsing, JD analysis, relevance matching, Claude-based tailoring (with an offline heuristic fallback), Jake's Resume LaTeX rendering, one-page enforcement and hyperlink validation.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "pdfjs-dist": "^4.6.82",
    "pdf-lib": "^1.17.1",
    "docx": "^9.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
